import { PropertyEditForm } from '@/components/admin/PropertyEditForm';

export const metadata = {
  title: 'Нов имот · Alma Carita PMS',
};

export default function NewPropertyPage() {
  return <PropertyEditForm property={null} />;
}
